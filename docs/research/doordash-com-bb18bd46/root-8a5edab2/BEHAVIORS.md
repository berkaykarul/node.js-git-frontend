# Behaviors

- Header: at scrollY > 48px a 48px black bar appears with logo + Sign In / Sign Up. Transition: appear instantly (source overlay).
- Address combobox: expands suggestions while typing. Find Restaurants (red 40px circle, #EB1700) submits.
- Sign in for saved address opens auth modal.
- Use current location fills a San Francisco address and opens store feed.
- Top Cities / Cuisines / Chains swap link grids; See more expands the same list.
- Hover: restaurant cards scale image 1.03 over 300ms; text links underline.
- Responsive: hero stacks; feed grid 1 / 2 / 3 columns; search bar moves under header on mobile.
- Cloudflare bot check can delay first load of the live site.
